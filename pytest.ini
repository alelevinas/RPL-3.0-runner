[pytest]
testpaths = tests
log_cli = true
log_cli_level = WARNING
console_output_style = count
addopts = --tb=short -rA
markers =
    integration: end-to-end tests that require language toolchains (gcc, python3, go, cargo)
